This is the interface to the old style mainframe, right?  I've set that up before at CAS, so it shouldn't be too hard.  However, I've never used it myself as you need access to the system so my experience only extends to the installation of the client.